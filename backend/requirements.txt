# Зависимости бэкенда (хватает и для разработки, и для деплоя на Render)
Django>=5.0,<6.1
djangorestframework>=3.15
django-cors-headers>=4.4
channels>=4.1
daphne>=4.1
whitenoise>=6.7
psycopg[binary]>=3.2

# Опционально (несколько процессов + Redis для общего channel layer):
# channels-redis>=4.2
